name: Publish Docker images to GHCR

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: ghcr-publish-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/sorteios

jobs:
  publish-frontend:
    name: Publish frontend image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend_worker
          file: ./frontend_worker/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:frontend_worker-latest
            ${{ env.IMAGE_NAME }}:frontend_worker-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  publish-backend:
    name: Publish backend image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend_worker
          file: ./backend_worker/Dockerfile.backend
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:backend_worker-latest
            ${{ env.IMAGE_NAME }}:backend_worker-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max